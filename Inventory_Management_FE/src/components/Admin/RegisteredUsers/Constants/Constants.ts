export enum successNotification {
    MESSAGE = "User Removed Successfully!",
    DESCRIPTION = "User is removed from the system successfully. Now that user can not interact with the system."
}

export enum failedNotification {
    MESSAGE = "User is not removed.",
    DESCRIPTION = "User can not be removed. You might not have privileges to remove the user."
}